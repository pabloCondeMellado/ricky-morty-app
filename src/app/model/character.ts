import { Location } from "./location";


export class Character {
    Character(  
        id: number,
        name: string,
        species: string,
        type: string,
        origin: Location,
        location: Location,
        image: string,
        url: string,
        epidodes: string[],
        created: Date

    ){
      

    }
}
